# Awen Diagnostic Build

This project has the experimental agent teams feature enabled via `.claude/settings.json`.

## Agent Teams

The `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` environment variable is set to `1`, enabling Claude Code to dispatch work across specialized sub-agents for multi-step tasks.
